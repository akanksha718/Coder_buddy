TECH USED
<br>
(uv)- ultrafast virtualenv/package manager written in rust much faster then pip
<br>
(lang-graph): built by langchain and used to create ststeful,graph-based Ai application it is a way to connect ai steps (nodes) in a graph instead of a straight line each node does some work passes its output to the next node based on logic